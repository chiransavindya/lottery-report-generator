services:
  app:
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    volumes:
      - ./server/.env.production:/var/www/server/.env:ro

  nginx:
    volumes:
      - ./docker/nginx/default.prod.conf:/etc/nginx/conf.d/default.conf
